- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.config/raycast: raycast

- create:
    - ~/Work

- shell:
  - 
    command: ./setup_homebrew.zsh
    stdin: true
    stdout: true
    stderr: true
 
